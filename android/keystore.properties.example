# 安卓签名配置
# 复制此文件为 keystore.properties 并填入你的实际值
# （keystore.properties 已加入 .gitignore，不会被提交）

storeFile=release.keystore
storePassword=your_store_password
keyAlias=release
keyPassword=your_key_password
